export const api_url = "http://vision.thefuturevision.com:7000/api";

export const config = {
  headers: {
    "Content-Type": "application/json",
    // "Authorizarion": JSON.parse(localStorage.getItem("auth"))?.accessToken
  },
};
